- 创建 auto-parts-dashboard 第一阶段版本：React+Vite+TypeScript Dashboard。
- 实现汽车零部件公司池、MockProvider、analytics 指标计算与深色投研 UI。
- 完成一年走势、成交额图表、收益率排名、趋势判断与更新按钮。
- 添加 README、.env.example 与 MCP 配置模板，为后续 Tushare MCP 接入预留 adapter。
- 第二阶段新增本地缓存与数据状态管理：实现 LocalStorage 缓存、Mock 增量更新逻辑、数据覆盖区间与数据管理面板。
- 第三阶段新增 Express + SQLite 后端：实现 SQLite 初始化、REST API、Mock 数据写入、Tushare Provider 占位与前后端联调脚本。
- 第四阶段完成真实 API 接入与错误处理：前端直接读取 SQLite API，新增 token / 非交易日 / API 连接错误提示，并完善 README 与部署说明。
- 新增自动更新调度器、Bloomberg/Wind 风格终端增强 UI、行业状态 Banner 与 AI 综合评分模块。
